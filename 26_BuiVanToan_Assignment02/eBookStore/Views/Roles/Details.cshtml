@model BusinessObject.Role

@{
    ViewData["Title"] = "Role Details";
}

<h1>Details</h1>

<div>
    <h4>Role</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.RoleDesc)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.RoleDesc)
        </dd>
    </dl>
</div>
@if (ViewData["Roles"] != null)
{
    <div class="row ml-0 mr-0">
        <p class="text-danger">@ViewData["Roles"]</p>
    </div>
}
<div>
    @if (Model != null)
    {
        <a asp-action="Edit" asp-route-id="@Model.RoleId">Edit</a> @Html.Raw("&verbar;")
    }
    <a asp-action="Index">Back to List</a>
</div>
